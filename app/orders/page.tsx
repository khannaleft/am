
import OrdersPageClient from './OrdersPageClient';

// This page is now simpler and just renders the client component
// that displays the user's order history.
export default function OrdersPage() {
  return (
    <OrdersPageClient />
  );
}